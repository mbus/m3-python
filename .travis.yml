sudo: false
language: python

python:
  - "2.7"

    #  - "2.6"
    #  - "2.7"
    #  - "3.3"
    #  - "3.4"
    #  - "3.5"
    #  - "pypy"

addons:
  apt:
    packages:
      - socat


before_script:
    pip2 install PyMulator

install: pip install tox-travis
script: tox

